extends layout

block content

  div(class="container", ng-controller="OrderGenerator")
    div(class="page-header gw-page-header")
      h1(id="ordering") Great Wall Order Generation
    p(class="lead") Less time spent ordering means more time drinking and discussing the price of bitcoin.

    input(type="number", placeholder="Enter number of people", ng-model="people", ng-change="refresh()", class="form-control input-lg")

    div(class="alert alert-danger", ng-if="error", ng-bind="error", ng-cloak, style="margin-top: 10px")

    ul(class="list-group", style="margin-top: 10px")
      li(class="list-group-item", ng-repeat="ordered in items")
        b(ng-bind="ordered.item.code", style="padding-right: 10px")
        span(ng-bind="ordered.item.name")
        div(style="float: right")
          span(class="label label-default") {{ ordered.quantity }} {{ ordered.size }}

    div(align="center")
      button(type="submit", ng-click="refresh()", ng-disabled="loading", ng-if="people > 0", ng-cloak, class="btn btn-primary") Mmm.. try again

    div(class="panel panel-default", style="margin-top: 10px")
      div(class="panel-body").
        If you feel like there's not enough chilli chicken, submit a pull request <a href="https://github.com/rowanseymour/greatwall">here</a>.
        Page built with <a href="http://nodejs.org">Node</a>, <a href="http://expressjs.com/">Express</a>, <a href="http://jade-lang.com/">Jade</a>,
        <a href="http://bower.io">Bower</a> and <a href="http://angularjs.org">Angular</a>. Such Javascript.
